version: '3.9'

services:
  auth:
    image: ghcr.io/myorg/service-auth:latest
    env_file: .env
    ports:
      - "8001:8000"
    networks:
      - app-net

networks:
  app-net:
    driver: bridge
